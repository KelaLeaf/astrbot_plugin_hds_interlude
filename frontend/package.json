{
  "name": "hds-interlude-console",
  "private": true,
  "type": "module",
  "description": "HDS Interlude 插件控制台（AstrBot WebUI 插件页面）",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test:unit": "node --experimental-strip-types scripts/check-select.ts",
    "icons": "node scripts/fetch-icons.mjs"
  },
  "dependencies": {
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "@tailwindcss/vite": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "typescript": "^5.9.3",
    "vite": "^7.1.7"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
