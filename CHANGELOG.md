# 更新日志

倒序排列。每条只写用户能感知的变化，实现细节在 `docs/PORTING_NOTES.md`。

## v1.3.4

适配 SearXNG：局域网自建实例可以直接当搜索源（模板填上即生效），正文提取不再被语言下拉之类的装饰节点淹没。

## v1.3.3

搜索地址模板以前没人读（宿主没有搜索接口时会静默留一条空白观察），现在按模板真去抓一页，抓不到记失败。

## v1.3.2

配置页写明推进节奏的优先关系：`urge` 打开会顶掉固定间隔，日程锚点会把推进提前到日程块的边界。

## v1.3.1

「仅处理名单内」三个开关收进各自的名单卡里，删掉与标题重复的说明。

## v1.3.0

账号过滤总闸删除，换成三张名单各自的「仅处理名单内」开关（默认关闭），所有平台一视同仁。升级后如果原来只处理白名单，需要手动打开对应开关。

## v1.2.18

把白名单的实际行为写进了配置提示与 README：闸门关闭时名单完全不参与判定，群聊与私聊是两条独立的线，名单也只对 OneBot 家族生效。

## v1.2.17

群聊消息没被接入时，日志会说明卡在哪一道门（没列进名单、列了但被关掉、没 @ 到、意愿分不够），按群节流十分钟。顺带修掉日志重复输出。

## v1.2.16

模型把回复直接写在正文、没写 `<say>` 标签时，不再整段丢弃，也不再白重写一次。

## v1.2.15

连发的消息会合并成一个回合。之前有一处字段拼写不一致，第二条消息到达时打断不了在跑的回合，会另起一回合。

## v1.2.14

「对方正在输入」这类通知不再刷屏；被丢弃的草稿会在日志里说清原因（模型写了什么、解析出几个动作）。

## v1.2.13

图片消息不再因为适配器给的是本地路径而整条丢掉；「未生成回合」的日志补上门禁原因与内容预览。

## v1.2.12

继续修「另一个她替我回答」：图片单独成一条消息时消息链为空，现在退回原始段解析；私聊看了却没生成回合时也吞掉事件，不再漏给别的 Agent。

## v1.2.11

归我们管的私聊不再落到宿主里第二个 Agent 手里。即使这一轮说不出话，也吞掉事件并留一条可见日志。

## v1.2.10

控制台「承诺与意图」不再列出宿主自己的调度记录（拆分气泡、叙事重试），默认折叠，可以勾选显示。

## v1.2.9

修掉一次白重写：模型用 camelCase 写引用时解析不出来，两个气泡的回合会被判成没有可见回复，白烧一次主叙事调用再重试。

## v1.2.8

控制台说清哪一部是主剧本，并加上「设为主剧本」，让你自己挑底座。

## v1.2.7

修掉「每个人的私聊各一条剧本」：上游这一代把共享主剧本硬开启，本移植版漏了对应的解析函数。

## v1.2.6

修掉「开启 Urge 后每五分钟自动写一段」：四个推进窗口填 0 被当成 1 分钟，现在 0 表示按档位。

## v1.2.5

带候选项的数字型配置在下拉框里能正确显示当前值。

## v1.2.4

22 个配置分组补上短标题，下拉框与卡片标题不再拖着一长串说明。

## v1.2.3

配置页每个字段统一成一张卡片，嵌套层级用底色区分。

## v1.2.2

控制台的下拉框改成自绘控件，展开后与整体界面一致。

## v1.2.1

配置页导航换成下拉框，并加上只读的「配置总览」，一眼看完 22 个分组的当前值。

## v1.2.0

控制台新增「配置」页。宿主配置页编不了的项（对象行列表、嵌套对象、对象行表格）也能在这里改。

## v1.1.2

修掉「运行开关」里嵌在模型中心下的四个开关显示与实际相反，以及往配置文件写无效键的问题。

## v1.1.1

修掉「提示词」组写了不生效：它与模型中心各有一份同名输入框，插件只读模型中心那一份。

## v1.1.0

新增配置导出与导入。入口在插件页面「配置备份」，不在聊天命令里。

## v1.0.0

全量移植上游 `1.0.1-beta6-rebuild`。这一代以持续剧本为唯一叙事主链：用户消息被当作进入主角生活的真实外部事件，她可以看见、可以不看、可以晚点回；回复由同一次行动的投递结果确认。配置、数据层、提示词、投递账本按上游逐模块搬过来，命令表仍是上游那 32 条。
