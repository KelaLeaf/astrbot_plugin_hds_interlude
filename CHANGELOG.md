# 更新日志

## v1.3.0

**「账号过滤」总闸拆成三张名单各自的开关，并改成对所有平台生效**（用户要求的配置灵活性）。

原来只有上游那一个总闸 `qq_access.enabled`，语义别扭且不对称：关着的时候私聊谁都能聊、**群聊却一个都不接**（填了群号也没用），开着的时候空白名单＝全部拒绝，而且只对 OneBot / NapCat 生效。现在：

- **总闸删掉**，三张名单各自一个「仅处理名单内」开关（默认**关**）：
  | 开关 | 关闭（默认） | 打开 |
  | --- | --- | --- |
  | 机器人账号名单 | 名单只作备注 | 只有名单里的登录账号收消息 |
  | 用户名单（私聊） | 名单外的人也进叙事，走「故事档案」的默认资料与关系 | 只有名单里的人能私聊 |
  | 群聊名单 | 名单外的群也接，按默认群规则处理（不 @ 就不说话） | 只有名单里的群会被接入 |
- 名单里的行仍然做**针对性处理**（称呼 / 背景 / 初始关系 / 群触发方式与节奏），名单外的走默认设定——两件事不再互相绑架。
- 三张名单**互相独立**：不再有"机器人和用户必须同时命中"的耦合；群聊仍然不看用户名单（群成员不需要私聊授权）。
- **所有平台一视同仁**：telegram / webchat / 官方 QQ 也受这套名单约束（以前只有 OneBot 家族受管）。
- 一行启动日志会说明现状：`接入与名单：机器人账号名单 1 条→仅名单内；用户名单（私聊） 1 条→名单外也接；…`；"开了仅名单内但名单是空的"这种一定不生效的组合用 warn。

**升级注意**：旧的 `qq_access.enabled` 键被删除（AstrBot 加载时会按 schema 清掉它），而三个新开关默认是**关**。如果你原来是"只处理白名单"的用法，升级后请在「幕间控制台 → 配置 → 接入与名单」里把对应的「仅处理名单内」打开。群聊则**不再需要**任何总闸：名单里没有的群现在也会被接入（按默认群规则，只在被 @ 时说话）。

受控偏离与上游断言的替换逐条记在 `docs/PORTING_NOTES.md` §22。

## v1.2.18

**白名单语义写进了配置提示与 README**（用户按自己的理解复述了一遍，发现几处和实际行为不一致）。

- 最容易踩的一条：**闸门关着的时候，白名单是完全不看的**——不是"名单内针对性处理、名单外走默认提示词"。
  代码在 `if not config.get('enabled'): return True` 就返回了，白名单连读都不读；不在名单里的消息
  HDSI 一律不管，会被交回管线由别的插件（或宿主默认 Agent）应答。提示词也只有全局一套，不按账号切换。
- 第二条：**群聊跟这个开关不是一回事**。闸门关着时群聊**永远不会**被接入（上游
  `canHandleGroupSession` 与 `canHandleSession` 在这里刻意不对称，`service.ts:955`）；
  开着时群号必须在 `group_chats` 里，且**群成员不需要在 `user_accounts` 里**（那是私聊授权）。
- 第三条：**闸门只对 OneBot / NapCat 家族生效**，telegram / webchat 等平台一律放行。
- 改动：`qq_access.enabled` 的 hint 与「幕间控制台 → 配置」里对应字段的说明改写成一句话说清
  （开关 × 私聊 / 群聊的对照），`plugin/README.md` 补一张五行真值表 + 三条要点。
  这几处文字直接抄自真实 service 跑出来的判定结果，不是复述文档。
- 回归测试 +1：`test_service_base.WhitelistSemanticsTests` 用九行真值表把整套语义钉住
  （闸门关时白名单完全不看、群聊永不被接入、群聊不看用户白名单、非 OneBot 平台不受影响）。

## v1.2.17

**群聊"完全没生效"现在说得出口了**（用户 2026-09-26：群里 @ 了机器人、群主也说了话，
HDSI 全程一个字都没有——没有回复，也没有任何解释）。

- 现象是全静默：消息进得来（`plugin -> astrbot_plugin_hds_interlude - on_group_message`），
  但日志里既没有拒绝原因也没有跳过原因。两处原因都看不见：
  ① `main.on_group_message` 判定不通过就**直接 return**（连一行 debug 都没有）；
  ② core 里那几条拒绝报告走的是 `diagnostic` 频道，而 `logging.verbosity` 默认 `standard`
  ——等于没有（同坑 25 / 45 的教训）。
- 现在每一道门都会说清楚：
  - **闸门**（`qq_access.enabled` / `bot_accounts` / `group_chats` / 群规则 `enabled=false`）
    逐条给出**具体原因**与**该怎么改**；"没列进白名单"与"列了但被关掉"分得开（修法不同）。
  - **建了回合但没推进**：`mention-only` 而这条没 @、找不到剧本、剧本不是 active、
    桌面端 paused、**意愿分数没到阈值**、还在冷却——都留一条可见说明（意愿那条最常是
    "配好了但一直不开口"的真凶，以前只在 diagnostic 里）。
  - **启动时**也打一句结论：接入了哪几个群 / 为什么没接入（没接入用 warn）。
- 这些行按「群 + 原因」**节流 10 分钟**：群消息是持续的，不节流的 warn 会把日志淹掉
  （见坑 45）；原因变了会立刻重新打一条，排查时能看到门是一道道关上的。

**顺带修掉一个把日志变成双份的老 bug**：`write_report` / `write_standalone` 先调
`log_layered`（渲染 + 投递一次），再调 `emit_log`；而没有宿主 logger 时 `emit_log` 走的
`log_fallback` 会把**已经渲染好的文本**当成原始消息再渲染投递一次——于是每条 report /
standalone 日志都出现两份，第二份还是嵌套的。现在渲染与投递分开：渲染用纯函数
`format_layered_log`，投递只在 `emit_log` 里做一次；控制台「运行日志」面板由适配层的
logger 适配器补一份（照旧剥 ANSI 色码）。

回归测试 +5（每一道门都报得出自己的原因 / 原因与布尔判定永远同源 / 节流但换原因会重报 /
启动自述点出缺哪一项）。

## v1.2.16

**模型不写 `<say>` 标签时，回复不再被扔掉**（用户 2026-09-26 11:44 的日志：那一轮她压根没回上话）。

- 上一版加的可见诊断直接把指纹打了出来：
  `interaction = {"mode": "none", "actionId": "reply", "unresolved_action_id": "reply"}`、
  **已解析动作=0**、**残留say标记=0** —— 模型**一个 `<say>` 标签都没写**，只把回复裸写成
  剧本里的一段气泡（`甲<sep/>乙<sep/>丙`），后面还跟着「发出去，我把手机搁回桌面上」。
  它照旧声明了 `interaction.reply.actionId = "reply"`。
- 后果比之前那次重写更糟：零已授权动作 ⇒ 上游的 `soleActionReply` 兜不住（它要求"恰好一个
  动作"）⇒ 判成「结构化可见回复缺失」→ 白重写一次 → **重写又犯同样的错** →
  `模型调用失败 … after one recovery attempt` → 整套 60 秒自动重试。她那一轮就是没回。
- 修法（受控偏离，`PORTING_NOTES.md` §20）：**整份剧本里只有一段含分隔符、且那一段本身就是
  合法气泡块**时，把这一段取回来当回复。判定与位置无关（块后面常有叙述），
  并且**只在**「单私聊接收者 + 明确 immediate 引用 + 零已授权动作 + 回复没有内容」时生效。
  内容是从散文里**取回**的，不是我们编的——「绝不从散文里猜动作」这条底线没动：
  出现两段候选（歧义）、空气泡、带尖括号、没有分隔符，一律不认，退回原来的重写路径。
- 认下之后把引用清空（`action_id` / `actionId` = None），否则 `resolve()` 会按"引用落地不了"
  把刚取回的内容又擦掉。

回归测试 +2（裸气泡块能落地且不再触发重写 / 四类歧义与畸形一律不认）。

## v1.2.15

**连发的消息终于合并成一个回合了**（用户 2026-09-26 00:22 的日志：汐雨. 连发「行吧」「晚安」
+ 一张晚安贴图，被拆成两个回合 —— 贴图那一回合她已经"睡着了、没看见"）。

- **根因是一处拼写失配**：缓冲回合 dict 里同一个字段有两种拼写（`base.py` 用 camelCase、
  `config.py` 的 TypedDict 用 snake_case），而 `_turn_set` 原来是"**跟随 dict 已有的拼写**"。
  `bufferUserNarrative`（Chunk2）建 turn 时只写了 `story_id` / `participant_id` / `messages` /
  `next_revision` / `obsolete_request_ids`；`in_flight_request_id` 是
  `flushBufferedNarrative`（Chunk3）**第一次**写进去的 —— 当时 dict 里没有 snake 键，
  于是它落在 camelCase。而 Chunk2 的两个"新消息到达"守卫直接读
  `turn.get('in_flight_request_id')` → 拿到 `None` → `shouldSupersedeNarrativeRequest`
  **永远为假**。于是：
  - 在飞回合不会被判过时（日志里既没有「新消息到达且首条回复尚未提交，放弃旧请求」，
    也没有「已丢弃过期主模型结果」）；
  - 它照常落库、推进游标，下一条消息只能另起一个回合。
  上游那套"新消息打断在飞请求 → 批次放回 → 合成一个事件"因此静默失效。
- **修法**：`_turn_set` 改为**两种拼写都写**（回合 dict 是纯内存结构，从不落库也不进 payload，
  多一个键零代价）；Chunk2 的三个守卫（`signal_incoming_interruption` /
  `buffer_user_narrative` / `deliver_early_private_reply`）改用共享的双读双写
  （`helpers._turn_get` / `_turn_set` / `_obsolete_request_ids`）；`base.py` 的暂停恢复路径
  同样双读。`_obsolete_request_ids()` 保证两种拼写指向**同一个 set**，避免"标记写进 camel、
  读取只看 snake"的下一轮事故。
- 修复后的行为：贴图到达时上一回合正在跑模型、且首条回复尚未提交 → 该请求作废、批次放回 →
  三条消息（两条文字 + 贴图）合成**一个**回合，她会在睡前看到那张晚安贴图。

回归测试 +1：`test_a_turn_field_written_by_flush_is_readable_by_chunk2_guards`
（用**生产写入方**的写法造 turn，钉死两个守卫都能看到在途请求号）。

## v1.2.14

**输入状态通知不再刷屏，回合被白重写时日志说得出为什么**（用户 2026-09-25 的日志）。

- **`input_status` 通知被当成消息**：NapCat 的「对方正在输入…」/停止输入是 OneBot 的
  `notice`（`post_type: 'notice'`），宿主照样派到消息处理器上，于是每条都走"空内容"那条路，
  打一条 `私聊事件没有可用内容` 的 **warn**——实测一分钟能刷十几条，把真正该看的东西全盖掉了。
  现在按非消息事件静默处理（`notice` / `meta_event` / `request`），消费策略与过去逐字一致
  （只有"归我们管的私聊"才吞，空事件仍然不会漏给后面那个人格），只留 debug 痕迹。
  别的平台（telegram / webchat / 官方 QQ）的原始事件里没有 `post_type`，**不受影响**。
- **`<say>` 标签写法容错**（受控偏离，`PORTING_NOTES.md` §19）：上游正则只认
  `<say id="x">` 一种字面写法，而模型会写 `<say id='x'>`、`<say id=x>`、`<SAY ID="x">`、
  `<say  id = "x" >`、全角空格，或者因为 JSON 转义层次写成 `id=\"x\"`。这些写法一个动作都
  解析不出来 → `interaction.reply` 的引用落地不了 → 已经写好的整个回合被判成
  「结构化可见回复缺失」**白重写一次**（多花一次主叙事调用 + 十几秒延迟）。
  现在只放宽标签的书写形式；id 字符集（ASCII `[\w-]`，1–64）、长度上限、
  「重复 id 不可执行」「绝不从散文里猜动作」这些约束**一条没放宽**，有反向测试钉死。
- **抛弃草稿的诊断改成可见**：原来那条 `被抛弃草稿的结构化回复字段` 走 `diagnostic` 频道，
  默认 verbosity 下一个字都不打（坑 25），用户只能看见干巴巴的「已抛弃本次未落库剧本并重新
  写作」。现在它走 `standard`（warn），并带上 **interaction 原文、已解析动作数、
  散文里残留的 `<say>` 标记数与预览**——解析成功时标签会被整个解包，所以"残留 > 0"就是
  "模型写了行动但一个都没解析出来"的指纹，下次再遇到直接看这一行就够。

回归测试 +7（通知不刷屏且别家平台不受影响、容错八种写法、拒收条件没放宽、容错后不再触发重写、
残留诊断、可见诊断）。

## v1.2.13

**图片消息不再被整条判死**（用户 23:35 的日志：事件被吞掉了，但什么也没发生）。

- 日志里那条 `私聊事件未生成回合` 说明 `receive()` 返回了 False。逐条核对它的门：
  文字正常、内容非空（135 字符），问题出在**视觉门**——当适配器把图片落成本地文件
  （`file:///…` 或绝对路径）时，图片源提取只认 http/data（上游 Koishi 的 `<img>` 永远带
  CDN 地址），于是"既无文字也无来源"→ 整条消息被判死。
- 现在**适配器给的元素**里的本地路径会被当成图片源（`onebot-file:`），
  `fetch_native_image` 在没有适配器 resolver 时直接读本地文件；
  ⚠️ **正文里的路径仍然拒绝**——任何人都能发一句 `<img src="/etc/passwd"/>`，
  从正文里认路径等于给模型开一个本地读文件的口子（有反向测试钉死）。
- 那条 warn 现在带 `原因=`（逐门诊断：白名单 / 剧本状态 / 参与者状态 / 无内容）
  与 `内容预览=`。core 里的"私聊未处理"走 `diagnostic` 频道，`logging.verbosity`
  不到 diagnostic 时**日志里一个字都看不到**——以后遇到这种"什么都不发生"，
  看这一条就够了。

回归测试 +4（适配器本地路径要认、正文注入的路径必须拒绝、http 两条路都正常、逐门诊断文案）。

## v1.2.12

**继续修「另一个她替我回答」**（v1.2.11 只堵住了一半——用户升级后又贴了一次日志）。

- **图片事件的结构化消息链为空**：宿主给的事件里 `message_obj.message` 是空的（图片单独成
  一条 QQ 消息时实测如此），于是我们既看不到那张图、又把它当成"没内容"漏了出去。
  现在**退回 `message_obj.raw_message`**（aiocqhttp 直接放了 OneBot 事件对象，`raw['message']`
  是原始段数组）重新序列化：图、语音、文件、@ 都能照常进叙事。
- **看了却没生成回合时也吞掉**：故事暂停、参与者不在、适配器形状怪…… 只要这条私聊归我们管
  （私聊 + `capture_direct_messages` + 白名单通过），`receive()` 返回 False 时同样
  `stop_event()`，并打一条**可见** warn（`概要=… 消息段=… 内容长度=…`）。
  注意 core 里那些"私聊未处理"是 `diagnostic` 频道，`logging.verbosity` 不到 diagnostic
  时日志里**什么都看不到**——所以适配层自己留痕。

回归测试 +3（空链回退原始段 / 原始段的文字与语音 / 看了没生成回合要吞掉）。

## v1.2.11

**修复：同一段私聊里冒出第二个人格回答**（用户贴日志报上来的：他给凌梦发了一张图 +
一句「你自己看吧」，结果另一个"她"回了「主人这是夜班的宵夜吗？看着好清淡呀…」）。

- 那不是我们的模型：那条回复来自**宿主里的第二个聊天 Agent**（AstrBot 默认 Agent /
  别的拟人插件）。我们的 `handle_event` 在"没有可用内容"时按上游语义 `next()` 交回，
  于是事件落到默认 Agent 手里；这一条路径在 AstrBot 部署里等于"把私聊让给别人"。
- 现在：**私聊 + `capture_direct_messages` 开着 + 通过白名单**的事件，即使说不出话
  也**吞掉**，并留一条 warn 说明（`概要=[图片] 消息段=0` 这类信息能直接看出是哪条漏的）。
  群聊、未接管的私聊、管理命令仍按上游语义交回其它处理器。
- **我们自己出错也不交回**：`on_private_message` 里任何异常都记 `error` 日志并吞掉事件
  ——一次崩溃不该变成"这段私聊换个人格来答"。
- 顺带确认：仅图片（没有文字）的私聊**照常进叙事**（有回归测试钉住），附件是合法输入；
  若某条图片事件的消息链在适配器侧就是空的，现在会留下上面那条 warn。

同一条日志里另外两件事**是正常的**：23:00:59 那次「结构化可见回复缺失 → 重写一次」
是模型没写 `<say>` 外壳（v1.2.9 的受保护行为，第二次就发出来了）；Urge 从
`conversation-density-decay` 切到 `rest-window`（下次 01:39 / 02:40）是夜里休息窗口生效。

回归测试 +3（空内容私聊要吞掉、空内容群聊照旧交回、capture 关闭时不吞、仅图片照常进叙事）。

## v1.2.10

**「承诺与意图」不再混进宿主自己的调度账**（用户指着那一列问「这些有问题吗」）。

- `split-message`（拆分气泡的投递节拍："她还在打字"）与 `narrative-retry`（叙事调用失败后的
  自动重试排程）是**宿主调度**，不是"她答应了什么"。用户看到整列
  `The character is still typing the next message segment.` 只会以为坏了。
- 现在这两种类型在接口里带 `internal: true`，控制台「承诺与意图」**默认折叠**，
  右上角给一个「显示内部调度（n）」勾选框；上面那张统计卡也只数人话层面的意图。
- 人话层面的意图（承诺回访 / 主动联系 / 延迟回复 / 剧情余波 …）一条不动，照旧显示。
- 顺带说明：你截图里那两条 `narrative-retry … completed` 正是 v1.2.9 修掉的「两个 `<say>` 块
  白重写」留下的痕迹——升级后不该再出现新的。

回归测试 +2（内部类型的标记必须准确、没有人话意图时也要回空数组）。

## v1.2.9

**修复：`<say>` 引用用 camelCase 时解析不出来——模型写了两个气泡就白重写一次。**
（用户贴日志报上来的，4 个私聊回合里有 2 个中招。）

- 模型的 `interaction.reply.actionId` 是 camelCase，而 `resolve_authored_actions` 内部只读
  snake_case `action_id`；`narrator.decide()` 交给它的又正是**模型原样返回的 JSON**
  （另外几条路径才先过 `_dual()` 转换）。于是「模型把两个气泡写成两个 `<say>` 块」的回合
  解析不出原话——`sole_action_reply` 的兜底只在**恰好一个** `say` 动作时生效。
- 后果：上层判成「结构化可见回复缺失」→ 白花一次主叙事调用重写；重写还不合格就整套
  60 秒自动重试（日志里 `模型调用失败 … after one recovery attempt` + `次数: 1/6`）。
  实测那两次失败各烧掉 ~1 万 prompt token、把回复推迟到 3 分钟后。
- 修法：`resolve_authored_actions` 的引用读取**两种拼写都认**（`actionId` / `action_id`），
  与文件里其它双读一致。

**顺带：声明了 immediate 却没写 `<say>` 时不再静默吞掉这一回合。**

- 上游对这种"引用了不存在的 say 动作"的回合会退成 `mode:'none'`（她读了却不回），
  用户看到的就是"这条她压根没理我"。
- 本移植版留一个内部标记，让**既有的「重写一次」机制**接手：不编造内容、不产生第二条回复，
  只是再要一次合法结构。主动不回（模型自己写 `mode:'none'`）不受影响。

**顺带：把「记忆整理看不到对话内容」说清楚。**

- 共享主剧本硬开启后每句私聊都挂在参与者上；`share_participant_details`（默认关闭，隐私优先）
  会让场景压缩把对话正文换成占位串 —— 摘要就成了「条目 32–46 的内容因隐私设置被省略」、
  长期事实 0（上游语义，刻意如此）。
- 现在压缩时会打一条 warn 说明原因与开关；`share_participant_details` 的 hint 也补上这个副作用。
  单人使用建议打开它（关系字段依旧匿名）。

回归测试 +8：camelCase/snake_case 双读、两个 say 动作不再误判、坏引用要标记成"重写一次"、
主动不回不重写、隐私开关两种取值下的压缩请求形状与告警。

## v1.2.8

**v1.2.7 的收尾：把「哪一部是主剧本」说清楚，并让你自己选底座。**

上一条只加了一个「并入主剧本」，但没说清哪部是主剧本、也没有"我想保留那部旧的"这个动作——
升级后库里出现多部旧剧本、一部 `character:` 都没有的中间态时，用户根本不知道从哪下手。

- **标签说清楚**：切换器里现在是 `凌梦 · onebot · 主剧本 · 128 条 · 进行中` /
  `凌梦 · qq · 旧剧本 · 4 条 · 已归档`。`主` = 真正的共享主剧本（active 且 `character:`）。
- **没有主剧本时会明说**：面板上方一行提示「还没有共享主剧本：选一部点「设为主剧本」…」。
- **新增「设为主剧本」**（`POST console/story-promote`）：选中的剧本（**含已归档的**）会先复活、
  再迁移成 `character:<平台>:<机器人>`，**继承它的设定与当前状态**（谁当底座由你定，
  不再由"谁的 updatedAt 最新"决定）。已有主剧本时该按钮不出现、接口也会拒绝
  （顶替要删掉现有主剧本那一行，属于毁数据，控制台不做）。
- **「并入主剧本」只在主剧本已定下来时出现**，目标固定是主剧本；没有主剧本时会提示先设一部。
- 顺带修掉一处判定：归档的 `character:` 剧本**不算**主剧本（否则「并入」会把内容搬进死档案）。

实测（复刻"31 条已归档 + 4 条进行中、无主剧本"的现场，走宿主 HTTP）：
`设为主剧本(31 条) → character:onebot:20000（32 条）` → `并入(4 条) → 38 条，两部旧剧本各转归档、0 条残留`。

回归测试 +7（设为主剧本、归档剧本复活、已有主剧本时拒绝、归档的 character 不算主剧本、
`main` 为空时的清单形状、切换器文案与两个按钮互斥的纯函数断言）。

## v1.2.7

**修复一：没有「共享主剧本」——每个私聊的人各自一条剧本**（用户实测报上来的）。

- 上游 `1.0.1-beta6-rebuild` 把共享主剧本**硬开启**（`get sharedStoryConfig` 里
  `enabled: true`，注释写明"Beta2 刻意保留单剧本守卫"；旧的回滚开关被丢弃）。
  也就是说：**一个角色一条时间线**，所有私聊的人在同一个剧本里各自是一条"关系分支"，
  旧的"每 QQ 一部剧本"由 `migrateLegacyStory` 惰性合并。
- 本移植版漏了 `resolve_shared_story_config`，读的是原始配置段、而那段没有 `enabled` 键，
  于是 `find_story` 一直走 per-user 分支——所以你看到的是"各人一部剧本"。
  现在按上游补上该解析函数（`enabled` 恒为真；写 `enabled: false` 同样不生效，与上游一致）。
- 升级后第一次有人说话时，旧的按账号剧本会被迁移成 `character:<平台>:<机器人>`：
  条目、场景、弧线、记忆、事实等一起搬过去，旧 id 转「已归档」（**内容不删**）。

**修复二：控制台只显示"最近更新的那部剧本"**（用户看到"新的人发消息前面的剧本就没了"）。

- 剧本面板加了**剧本切换器**（顶栏右侧，库里有 2 部以上才出现）：列出全部剧本，
  含 `archived`，标出「角色 · 平台 · n 条 · 状态」，可以切过去翻看旧剧本。
- 单剧本守卫会归档其余剧本，而归档后的旧分支上游**不会**再合并（`migrateLegacyBranchIntoShared`
  遇到 archived 直接返回）——所以给控制台加了「**并入主剧本**」按钮：把选中的旧剧本
  （条目 / 记忆 / 事实 / 场景 / 弧线）搬进当前主剧本，源剧本转归档，并在主剧本里留一条
  `legacy-branch-merged` 记录。这是**控制台专属能力**，上游没有这个入口，`findStory`
  的行为一行未改。

新增接口：`GET console/stories`、`POST console/story-merge`。回归测试 +11（含两个账号
共用同一部剧本、单剧本守卫归档、并入的参与者 id 必须真实、切换器文案的纯函数断言）。

## v1.2.6

**修复：开启 Urge 后剧本会「每 5 分钟自动写一段」**（用户实测报上来的）。

- 根因在配置默认值：`_conf_schema.json` 给 Urge 的四个推进窗口（`hot/idle/burst/slow` 的 min/max）
  写了 `default: 0`，hint 说「0 表示按档位」——但解析层照上游的 `finite()` 做「是有限数就夹到
  [1, 1440]」，**0 被夹成 1 分钟**，四个窗口全塌成 `[1, 1]`。于是每次写完 1 分钟后就"到期"，
  而后台扫描默认 5 分钟一轮 → **每一轮都判定到期，剧本每 5 分钟自动写一段**。
  （上游 Console 这几个字段没有默认值、下限定为 1，所以上游碰不到 0 这个值。）
- 现在 `≤ 0` 明确按"未设置"处理：回落到所选档位的默认区间（medium = hot 10–20 / idle 35–55 /
  burst 3–7 / slow 110–130 分钟）。窗口以外的字段保持上游语义（`burstBudget: 0` = 关闭加速、
  `burstThreshold: 0` = 任何 Urge 都算高、`jitter: 0` = 无抖动）。
- 既有配置**不用改**：文件里已经写下的 0 从此就按档位解释。
- 回归测试 4 项：三档 × 全 0 的窗口解析、显式值仍夹取、非窗口字段的 0 语义、以及"全 0 配置不再
  把下次推进排在 1 分钟后"。

## v1.2.5

**修复：带候选项的数字型配置在下拉里不显示当前值**（用户实测发现：「图片最长边像素」显示成空）。

- 根因是 v1.2.2 把下拉换成自绘控件时引入的：候选项是**数字**（`[0, 512, 768, 1024]`），
  而控件拿字符串化的当前值去比较（`1024 === "1024"` 为假），于是匹配不到任何候选项、
  按钮退回占位符。现在比较一律字符串化，但**选中后回传原始类型**（数字仍是数字），
  值不在候选项里时也会把当前值显示出来。

## v1.2.4

**配置分组有短标题了**（下拉里那行太长）。

- `_conf_schema.json` 的 22 个分组补上 `title`（「故事档案」「模型中心」「提示词」…），
  控制台的下拉、卡片标题、只读总览都用它——不再出现「模型中心：先添加连接并勾选用途，
  主叙事参数与高级模块同处。提示词四件套在「提示词」组。」这种塞满下拉的标签；
  完整那句话留作卡片副标题（去掉与标题重复的开头）。
- 测试钉住：每个分组必须有非空、≤16 字、不含句号的标题。

## v1.2.3

**控制台配置项的容器统一**（有的有框、有的没框，看着乱）。

- 以前只有「对象行列表」里的行有边框，标量字段没有、嵌套对象只有一条左边线——同一页里
  三种长相。现在**每个字段都是一张卡**（同样的边框与圆角），嵌套层级用交替底色区分
  （顶层浅一点、里层深一点），从属关系一眼可见。
- 行内字段不再重复套框（行本身已经是卡），避免框里套框套框。

## v1.2.2

**控制台下拉框换成自己画的，宿主页那句提示去掉重复的 ‼️。**

- 原生 `<select>` 展开的那一层是操作系统画的，跟整套 UI 不搭（用户直接指出"点开后没有样式"）。
  现在 `Select` 是按钮 + 自己画的面板：当前项打勾、↑↓ / Enter / Esc 可用、点外部关闭，
  props 不变，所以配置页与连接池编辑器一起变好看。
- 宿主配置页那句提示原本写成 `‼️⚠️…`，而 AstrBot 对 `obvious_hint` 自己会加一个 ‼️，
  结果显示成 `‼️‼️⚠️…`。现在 schema 里只留 `⚠️此配置项不生效，请在「幕间控制台 → 配置」处进行配置`。
- 控制台内部不再显示"去幕间控制台改"这类指向自己的提示。

## v1.2.1

**控制台配置页的界面收拾干净**（按使用反馈）。

- 分组导航从"一堆按钮"换成**下拉选择**，第一项是 **「配置总览」（只读）**：一眼看完 22 个分组、
  196 项现在是什么值（每条按类型显示：开关=开/关、列表=n 条、长文本截断），不会误改。
- 去掉页面上那几段解释性长文（字段上的"宿主页编不了…"说明、「为什么有些字段标着宿主页编不了」
  面板、备份页的"一个边界…"那条）——需要提醒的地方只在**宿主配置页**留一句话：
  `‼️⚠️此配置项不生效，请在「幕间控制台 → 配置」处进行配置`。
- 行为提醒（如"开启后空白名单等于全部拒绝"）与"连接池请在模型面板编辑"这类短提示保留。

## v1.2.0

**控制台新增「配置」页：所有可配置项都能在控制台里改**（AstrBot 自带配置页编不了的项也能改）。

- 起因：AstrBot 自带配置页把 `type: list` 渲染成**字符串数组**控件，行内字段定义会被忽略——
  「用户白名单」「群聊白名单」「机器人白名单」「模型连接池」「作息窗口」这类**对象行**列表在那儿
  编辑会把整行压成一个字符串（白名单会静默失效）。控制台现在按 `_conf_schema.json` 自己渲染：
  递归对象、**对象行表格**、标量标签、JSON 兜底，22 个分组 196 个字段都能改。
- 写入门禁 = **整份 schema**：只有 `_conf_schema.json` 里声明过的路径能写，路径不在 schema 里一律拒绝；
  类型按 schema 收（`true`/`1` 这种字符串也会被正确转换），清空则删键、回落到 schema 默认值。
- **兼容性提示**：编不了的那几个字段在控制台里标着「宿主页编不了」，并写明原因与替代做法；
  `_conf_schema.json` 里也补了同样的 `hint`（AstrBot 自带配置页会标 ‼️ 提醒）。
- **白名单一键填入**：新增参与者接口，把最近会话里的 QQ / 群号 / 昵称直接带进白名单行，
  不用再去翻自己的账号。
- 连接池仍走「模型」面板（那里不回显密钥）；配置页里对应的字段只给说明与跳转提示。
- 「备份 / 导入」原功能一字未减，跟「配置」页做成两个页签。

## v1.1.2

**修复：控制台「运行开关」里嵌在模型中心下的四个开关显示与实际相反，并把垃圾键写进配置文件。**

- 「后台压缩 / 语义检索 / 图片理解 / 语音理解」这四项在配置里嵌在**模型中心**下面
  （`model_center.compaction` 等），而控制台读的是顶层同名分组——顶层根本没有这几个分组，
  于是读出来的是默认值：**配置里开着的显示成关着、关着的显示成开着**（角色实际行为一直是对的，
  误导的是界面）。重启 AstrBot 之后必现，因为宿主会把历史遗留的假顶层键删掉。
- 点开关时，除了写对的那一处，还会往配置文件里塞一个 **schema 里不存在的顶层分组**
  （`vision` / `audio` / `embedding` / `compaction`）：宿主下次加载会当未知键删掉，它也会混进
  导出的配置文件里。
- 现在：读固定看模型中心里的那一份；写只写模型中心那一处，并顺手清掉旧版本留下的假顶层键。
- 顶层分组（主动可见消息 / Agency / Urge / 日程预排 / 时间导演 / 网页观察）行为不变。

## v1.1.1

**修复：「提示词」配置组以前是哑的，写在里面的内容不生效。**

- 配置页的「提示词」组（`prompts`）与「模型中心」（`model_center`）里各有一份同名同默认值的
  提示词输入框，而插件只读模型中心那一份——于是在「提示词」里写的东西**静默失效**。
  上游 Koishi 把提示词放在模型组里，这一组是本移植版单开的，重复项是移植时留下的。
- 现在「提示词」组是**唯一入口**：四个提示词（主叙事行为指令、结构化输出补充规则、通用长期规则、
  全局叙事文风）只在那一处编辑，插件读写时自动与内部键位对接。「模型中心」里不再有重复项。
- **旧配置不会丢**：内容只写在模型中心那份里的配置文件，导入时会自动搬进「提示词」组；
  两边都写过且不一样时，以「提示词」组为准。
- 若你之前在「提示词」组里写过内容——那些内容**从这一版开始才真正生效**，建议按需核对一遍。

## v1.1.0

**新增配置导出 / 导入**（上游 Koishi 没有，本移植版独有）。入口在 **WebUI 的插件页面「配置备份」**里，
不是聊天命令——配置的事归配置界面。

- 页面上「下载配置备份」把当前插件配置导出成一个 JSON 文件（浏览器直接下载，同时落一份到
  `<插件数据目录>/exports/`）。导出的是**磁盘上那份原样配置**（含你不认识的键），不做任何归一化。
- 「导入配置」选文件 → 先看**变更预览**（改哪些、增哪些、删哪些）→ 点「确认导入」才真正写盘。
  导入是**合并**：文件里没写的设置保持原值，不会被清掉。
- 导出的文件带 `format` / `formatVersion` / 插件版本 / 上游版本，所以**以后升级插件仍然能导回来**。
- 兼容契约（硬要求，见 `docs/PORTING_NOTES.md`）：
  - 带信封的新文件、早期版本没有信封的裸配置、手写片段、甚至更新版本插件导出的文件，**都能导入**；
  - 分组改名（`model_center` ↔ 上游 `model`）由配置别名统一处理，两种名字都认；
  - **未知键原样保留、缺失键补默认值**，绝不因为多一个键 / 少一个键拒绝整份配置
    （注意这一条覆盖的是插件的解析/合并/落盘链路；AstrBot 自己会按 `_conf_schema.json`
    重建配置，schema 里没声明的键在下次加载时会被宿主清掉）；
  - 迁移链只增不改：将来任何键改名都往 `core/config_io.py` 的 `_MIGRATIONS` 里**追加**一步。
- 导入写回时统一转成 **schema 分组名**（`model_center` / `qq_access`），这样 AstrBot 配置页
  能正确显示导入后的值——写上游名会让配置页看起来"全是默认值"。
- 命令总数仍是 **32 条**（与上游一致），配置功能没有占用任何聊天命令。

**模型可以按任务挑 AstrBot 里配好的了**（上游 Koishi 没有，本移植版独有）。

- 模型中心新增「主叙事模型」「压缩与总结模型」「Alter 分析模型」；「图片理解」里加「图片解析模型」，
  「语音 / 音频理解」里加「语音转写模型」，「本地表情包」里加「表情包描述模型」，
  Embedding 组里加「Embedding 模型」。**全部留空即使用默认 Provider**，与旧配置完全一致。
- `_special: select_provider`（AstrBot 官方配置页的模型选择器）就地下拉，不用手抄 id；
  语音那一项用 `select_provider_stt`，选的是语音转文字模型。
- **按 `modalities` 校验能力**：选中的模型声明里没有图片能力时，图片会被丢掉并说明原因
  （不是发出去让服务端报错——AstrBot 的 `text_chat` 在模型不支持图片时会直接抛错，整轮就废了）；
  没填 `modalities` 的模型照常发送，不猜。
- **主模型缺图片能力时会明说**：启动后（等 Provider 加载完）与 `hdsi_status` 里都会出现
  「当前主模型未声明图片能力，图片会被忽略（部分服务商会直接报错），建议改用 sidecar」。
- 指定了语音转写模型时，语音先转成文字再进主叙事 —— 主模型不需要支持音频。
- 指名了 AstrBot 模型的任务不再走「私聊首泡加速」（AstrBot 的生成接口没有增量通道），
  自动退化成整段返回，不会拿空地址去请求。
- **可以完全不填连接池**：只用上面那些「指名」的模型时，连接行留空也能跑（core 原先只认
  http `endpoint`，会把留空的连接判成不可用；现在由适配层给 core 补一条声明了
  `transport_target` 的合成行，core 不因此认识 AstrBot）。

**新增插件控制台**（WebUI 插件页面 `pages/console/`，取代原来的「配置备份」页）。

- 七个面板：**总览**（故事、参与者、开关、模型路由、能力提示）、**模型**（任务→模型来源、
  连接池、AstrBot 模型与能力声明、用量）、**剧本**（条目时间线、场景、弧）、
  **记忆**（长期事实、压缩记忆、承诺意图、设定演化候选、参与者）、**数据库**（13 张表行数）、
  **运行日志**（内存环形缓冲，可筛选、可自动刷新）、**配置**（导出 / 导入，功能与原来一致）。
- 后端在 `plugin/adapters/console_api.py`：只读、不返回任何密钥（endpoint 会去掉 query），
  每个面板在"没有剧本 / 没建表 / 没配模型"时都返回空壳而不是报错。
- 前端源码在 `plugin/frontend/`（Vite + Preact + Tailwind v4），**构建产物提交进仓库**，
  用户安装时不需要 Node。运行时依赖只有 Preact 一个，gzip 约 25KB JS + 4KB CSS。
- 图标取自 [Game-Icon-Pack](https://github.com/Nieobie/Game-Icon-Pack)（CC0），只挑了 30 个内联进产物。
- 后来又补了三个面板与两处写操作：
  - **Alter 情绪**：当前氛围位移、按来源分的桶、两条 SVG 曲线（累计位移 / 单轮增量）、触发配置与历史。
  - **Agency 日程**：日程负荷 / 隐私环境 / 设备可用三档容量条，窗口有效期与推断依据，日程预排摘要。
  - **投递账本**：按状态筛选，点开每条能看 commit / event / 每个分段的结果——排查"发了但对方没收到"看这里。
  - **运行开关**：10 个白名单开关点一下即刻生效（主动联系、Agency、Urge、压缩、语义检索、图片/语音理解…）。
  - **连接池编辑**：增 / 改 / 删连接，含用途勾选与采样参数；**密钥永不回显**，留空即保留原值。
- 「配置」面板的文件选择器换成自绘的可点击 / 可拖拽区域（原生 file input 跟其余 UI 不搭）。

## v1.0.0

**全量移植上游 `1.0.1-beta6-rebuild`（v2 剧本内核）**。v0.1.0 的"核心叙事子集"被整体替换：这一版把上游 Koishi 插件从配置、数据层、提示词到投递账本逐模块搬到了 AstrBot 上。

上游这一代的中心思想是**以持续剧本为唯一叙事主链**：原始剧本保存生活与人物理解，实际通信由同一行动的投递结果确认；用户消息不是"一条要回答的消息"，而是进入主角生活的**真实外部事件**，她可以看见、可以不看、可以沉默、可以晚点回。

### 叙事内核

- **时间感知**：故事时区 / 本地时间 / 时段与日照预期 / 用户陈述的时间引用，作为主叙事的权威时钟。
- **七段式上下文**：`storyIdentity → relevantEstablishedEpisodes → currentSceneEvidence → ongoingThreads → availableNearFuture → incomingEvent → authoringWindow`，附 cache-first 紧凑投影。
- **剧本提交**：一次写作产出 `ScriptCommit`（正文 + 事件 + 场景增量），宿主提交带 `frameId`/`burstId` 的确定性身份。
- **正文内 <say> 行动**：模型在正文里用 `<say id="…">原话</say>` 标记真实台词，宿主解出原话、按投递规范化发送，引用失配时保守兜底，绝不凭空造第二条回复。
- **SceneFrame / DialogueBurst**：当前场景的可重建导航（每一格都带来源条目 id），同一 scene 的消息共享身份，时间间隔不改变它。
- **投递账本**：每个已提交行动在剧本条目 metadata 内建 M6.1 账本，私聊/群聊/拆分气泡/贴图/原生表情/群表态共享 `commitId`/`eventId`，平台结果按 segment 回写 `pending | delivered | partial | failed | cancelled`；已 delivered 是终态，晚到的记账不得降级。
- **lifeHandoff**：用本轮原文引用交接地点、活动、在场与已完成小事，只作导航，不是第二份场景摘要。

### 记忆与内在

- **长期事实与证据**：事实带 `knowledge`（观察/转述/想法/提议/条件/确认 + 原文引用 + 认知主体），区分"她相信的事"与"实际沟通结果"。
- **记忆分层**：原始剧本 → 场景/弧摘要 → 长期事实 → continuity 快照（历史兼容）→ Overlay（稳定演化）→ Perspective（外壳人格层）→ active consequence。
- **场景压缩**：条目超阈值后台增量压缩，顺带更新在场表、working details、人物倾向候选。
- **Alter System**：可消退的内在天气，按来源分桶累计、按阈值触发侧端分析、带冷却与权重衰减。
- **Agency Window**：日程 / 隐私 / 设备构成的外部行动容量，只约束联系行动，不控制文风与情绪。
- **Urge**：以真实消息热度与已提交原文的 urge/slow 交接统一调度下一次推进（默认关闭，四档 + 自定义）。
- **Schedule Preplan**：未来近期的计划结构，程序保存多日，主叙事只读未来约半天，且不得把计划当已发生事实。
- **群聊意愿层**：半衰减分数 + 边际递减 + 阈值概率 + 成功发言成本，决定普通群消息是否值得进主模型；@ 机器人绕过概率门。
- **承诺回访**：用户回合里说出口的"想想再答复"落成带到期时间的 intent，到期必须给出可见履行、延期说明或取消说明。

### 能力

- **模型中心**：任意多条 OpenAI 兼容连接 + 任务级路由（`main` / `compaction` / `timeline` / `alter` / `embedding` / `stickers` / `vision`）、failover、round-robin、智谱官方 / DeepSeek 思考 / 百炼区域等预设模式、token 用量与费用统计。
- **多模态**：原生图片输入、QQ 语音与音频文件、侧端视觉描述模型、本地表情库（含语义检索与描述生成）。
- **网页观察**：模型提出浏览意图 → 宿主在并发闸门内取回文本摘要 → 作为上下文进入后续回合（能力由平台适配层提供）。
- **多平台**：私聊与群聊入站统一走适配层，`metadata.yaml` 的 `support_platforms` 覆盖 AstrBot 主流平台。
- **盲区模式**（上游原"失明模式"）：开启后不注册管理命令、运行日志收束为周期心跳。
- **桌面桥**：桌面端用的时间线 / 游标 / 暂停恢复等命令，本移植版做成可选桥。

### 数据

- **13 张 SQLite 表**，列名与上游逐字一致（`camelCase`），含增量补列与热重载不重建表的升级路径。
- **状态信封 v4**：版本化编解码 + 幂等升级，未知顶层键搬进 `extensions` 保留而不是写回时丢失。

### 命令

上游全部 HDSI 管理命令已移植（`interlude.story.*` / `participant.*` / `admin.*` / `memory.*` / `schedule.*` / `desktop.*` / `blind.*` 等）。
AstrBot 命令名不能带点号层级，因此统一改为下划线形式并保留原语义与**逐字中文返回文案**，
完整对照表见 `docs/COMMANDS.md`。

## 实测修复

移植期间在真机/实测中暴露并修掉的问题（都写进了 `docs/PORTING_NOTES.md`）：

- **AstrBot `_conf_schema.json` 不是标准 JSON Schema**：顶层直接是字段表；`type` 只能用 `string/text/int/float/bool/object/list/template_list/file`；`object` 子字段用 `items` 而不是 `properties`；文件带 BOM。写错会报 `TypeError: string indices must be integers`。
- **`on_private_message` 里事件属性必须用 getter**：`event.self_id` / `session_id` 在真机 `AiocqhttpMessageEvent` 上不存在，要用 `get_self_id()` / `get_session_id()` / `get_sender_id()` / `get_platform_name()`。
- **sqlite 单连接不是线程安全的**：`Database.write` 走 `asyncio.to_thread`，service 层又会 `asyncio.gather` 并发读，无互斥时实测 6 路并发读 × 200 轮约 7.5% 抛 `sqlite3.InterfaceError: bad parameter or other API misuse`。现由每个 `Database` 实例的可重入锁把**每一次**连接操作串行化（1200 次并发读 0 失败）。
- **`WeakSet` 语义不能在 Python 里用裸 `id()` 集合替代**：对象被 GC 后 `id` 立刻复用，会留下失效标记 → 本应沉默的回复被放行、或回复直接丢内容。改用带强引用 pin 的标记注册表。
- **JS 与 Python 的语义差异**（逐条实测对齐）：`Math.round` 是半值向上（Python `round` 是银行家舍入）；`isRecord({})` 在 JS 为真；`matchAll` 里 `[^\d]` 会吃掉一个字符而 Python 前瞻不吃；`??` 与 `||` 对显式 `0` 的语义完全不同（`userMessageDebounceSeconds ?? 2` 写成 `||` 会让"显式 0 = 不防抖"失效）；`Number.isSafeInteger(Number(null))` 为真。
- **测试的跨模块顺序污染**：`'astrbot' not in sys.modules` 这类全局断言会因别的测试先导入 astrbot 而误报；改为在干净子进程里只导入本包再检查。

## 与上游的差异

见 `README.md`「与上游的差异」。原则是：**行为以能跑通为最高优先级，凡是无法在 AstrBot 复现的能力都走显式降级分支并记录**，不留占位。
